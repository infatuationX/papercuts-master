
cc.Class({
    extends: cc.Component,

    properties: {
  
    },

    onLoad:function()
    {
        if(levelControl1 == 1)
        {
        this.node.active = false;
        }
    },

    start () {

    },

    // update (dt) {},
});
